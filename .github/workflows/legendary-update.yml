name: Professional Profile Update

on:
  schedule:
    - cron: '0 12 * * *'  # Run daily at noon
  workflow_dispatch:

jobs:
  professional-update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      
      - name: Update Professional Stats
        uses: anmol098/waka-readme-stats@master
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
          SHOW_PROFILE_VIEWS: true
          SHOW_LANGUAGE: true
          SHOW_OS: true
          SHOW_EDITORS: true
          SHOW_LOC_CHART: true
          SHOW_LINES_OF_CODE: true
          SHOW_PROJECTS: true
          SHOW_ACTIVITY_GRAPH: true
          SHOW_SHORT_INFO: true
          SHOW_LOC: true
          SHOW_TIMEZONE: true
          SHOW_COMMIT: true
          SHOW_DAYS_OF_WEEK: true
          SHOW_LANGUAGE_PER_REPO: true
          SHOW_GITHUB_STATS: true
          SHOW_DAILY_DEV_TIME: true
          SHOW_PROFILE_VIEWS: true
          SHOW_FOREST: true
          SHOW_FOREST_DETAILS: true
          FOREST_TOP_LANG_COUNT: 6
          FOREST_CHART_COLORS: '#8B5CF6,#10B981,#7C3AED,#6366F1,#4ECDC4'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} 